[pytest]
testpaths = tests
addopts = -v --html=report.html --self-contained-html
markers =
    schema: schema/key validation
    data: data-driven cases
    perf: response-time smoke
    car_ad_post: tests related to car ad lifecycle
